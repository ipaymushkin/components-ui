import React from 'react';
import styled from 'styled';

const Row = ({ children }) => (
        <Wrapper>
            {children}
        </Wrapper>
    );

const Wrapper = styled.div`

`

export default Row;
